{
  "name": "tunegie",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "concurrently --names \"API,REACT\" --prefix-colors \"blue,green\" \"npm run php-server\" \"npm run react-app\"",
    "react-app": "cd frontend && npm start",
    "php-server": "C:\\laragon\\bin\\php\\php-8.1.10-Win32-vs16-x64\\php.exe -S localhost:8000 -t .",
    "node-server": "cd backend/node && npm start",
    "build": "cd frontend && npm run build",
    "railway-build": "npm run build && node deploy/scripts/create-php-router.js && (php database/migrations/migrate.php || echo 'Migration skipped')",
    "deploy": "railway up",
    "test": "cd frontend && npm test",
    "install-all": "npm install && cd frontend && npm install && cd ../backend/node && npm install",
    "dev": "concurrently --names \"PHP,NODE,REACT\" --prefix-colors \"blue,yellow,green\" \"npm run php-server\" \"npm run node-server\" \"npm run react-app\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
